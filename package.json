{
  "name": "day-menu",
  "productName": "Day Menu",
  "version": "1.0.0",
  "description": "Osobisty panel: nastrój, sen, cele, nauka",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Day Menu\" --platform=win32 --arch=x64 --overwrite --out=dist --icon=build/icon.ico --ignore=dist --ignore=android-app --ignore=docs",
    "android": "node build-android.js",
    "publish": "node publish.js"
  },
  "author": "user",
  "license": "MIT",
  "devDependencies": {
    "electron": "^42.4.0",
    "electron-packager": "^17.1.2"
  }
}
